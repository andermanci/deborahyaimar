{
  "name": "ad-galeria-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:local": "wrangler d1 execute ad-galeria --local --file=schema.sql",
    "db:remote": "wrangler d1 execute ad-galeria --remote --file=schema.sql"
  },
  "dependencies": {
    "aws4fetch": "^1.0.20"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260830.1",
    "typescript": "^5.9.2",
    "wrangler": "^4.127.1"
  }
}
